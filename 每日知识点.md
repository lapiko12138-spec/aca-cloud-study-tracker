# 📖 ACA 每日知识点 · 学习笔记

> Valerie 备考阿里云 ACA 云计算认证的**每日知识点主文档**，由 AI 导师每天自动追加更新。
> 📱 手机查看：直接在 GitHub 打开本文件即可（可搜索、可跳目录）。
> 🗓️ 最后更新：2026-06-24

## 目录
- **Day 1（6/23）** 云计算概念 + ECS 入门

---

## Day 1 ｜ 2026-06-23 ｜ 云计算概念 + ECS 入门

### 一、核心概念 ✅
- **服务模型**
  - **IaaS**（基础设施即服务）：给你服务器/网络/存储，自己搭 → 例：ECS
  - **PaaS**（平台即服务）：给你运行环境/中间件 → 例：托管数据库
  - **SaaS**（软件即服务）：开箱即用的软件 → 例：在线 CRM、企业邮箱
- **部署模型**：公有云 / 私有云 / **混合云**（公私结合，按需分配业务）
- **地域 Region & 可用区 AZ**
  - 地域 = 不同城市的数据中心群；可用区 = 同地域内电力/网络独立的机房
  - 关键：**同地域多可用区延迟低 → 做高可用**；不同地域默认不内网互通
- **计费方式**：包年包月（长期稳定）/ 按量付费（短期·波动）/ 抢占式（最便宜，可能被回收）

### 二、核心产品速记表 🔑（重点，入学错题全在这）
| 缩写 | 全称 | 一句话 | 类比 |
|---|---|---|---|
| ECS | Elastic Compute Service | 云服务器，跑程序/网站 | 租来的电脑 |
| OSS | Object Storage Service | 对象存储，存图片/视频/备份 | 无限网盘 |
| RDS | Relational Database Service | 托管关系型数据库(MySQL等) | 别人运维的数据库 |
| SLB | Server Load Balancer | 负载均衡，分发流量 | 餐厅领位员 |
| VPC | Virtual Private Cloud | 专有网络 | 院墙/内网 |
| RAM | Resource Access Management | 访问控制，管权限 | 门禁卡 |
| CDN | Content Delivery Network | 内容分发，就近加速 | 连锁便利店 |
| NAS | Network Attached Storage | 文件存储，多机共享 | 共享文件夹 |

### 三、ECS 五段式精讲 ⭐（考试占比最高）
1. **概念**：阿里云机房里你独享的一台虚拟服务器，可选 CPU/内存/系统/硬盘，远程登录上去跑程序。
2. **为什么存在**：替代自购物理服务器——分钟级开通、按需付费、随时增减、宕机自动迁移；把"重资产采购"变"轻运营租用"。
3. **实际案例**：电商双 11 用弹性伸缩临时扩 100 台 ECS 扛流量，大促后自动释放，只为高峰那几天付费。
4. **高频考点**：ECS=IaaS/弹性计算；镜像=系统模板；快照=磁盘备份；安全组=虚拟防火墙；三种计费；规格族(通用/计算/内存/GPU 型)。
5. **面试提问**："ECS vs 自购服务器省在哪？" → 省钱(免硬件 CapEx)·省心(运维阿里云管)·弹(随业务增减)·稳(自动迁移+多 AZ 高可用)·快(分钟级上线)。

### 四、课时1 PPT 精华 📷（你的截图整理）

**① 传统 IT（自建机房）要操心的事 → 云为什么出现**
- 机房(电力/空调/机柜) + 硬件(服务器/交换机/路由器/防火墙/IPS/IDS) + 软件(操作系统/Web服务器/数据库) + 人员(施工/维护/DBA/系统/网络管理员)
- 痛点：**耗时耗力、人+设备成本高、不利于扩容**
- 本质：**传统IT＝购买硬件→部署硬件→部署软件→部署应用；云计算＝直接购买服务**
- 🔌 经典类比：像电力——100 年前各家自发电，今天直接用电网。云＝计算界的"电网"（计算/网络/存储三大资源池）

**② 部署模式（按"谁拥有、谁使用"分）**
- 专有云（＝私有云）：企业内部自建 ｜ 公有云：服务商提供、经互联网用 ｜ 混合云：内外互通，兼顾私密性+灵活性
- ⚠️ 术语坑：阿里云把"私有云"叫**专有云**

**③ 服务模式（可操控性 IaaS > PaaS > SaaS）**
- IaaS 基础设施即服务：给计算/存储/网络资源，自己搭系统，**可操控性最高**（ECS 即典型）
- PaaS 平台即服务：给开发者按需开发/测试环境
- SaaS 软件即服务：直接用厂商软件（如在线 CRM）

**④ 阿里云简介（高频记忆点）**
- 2009 年成立；服务 **200+ 国家和地区**；**2017.1 成为奥运会全球指定云服务商**
- 全球数据中心：中国华东、华北各约 15 个可用区（印证"一个地域含多个可用区"）

> 🎯 考点：①云＝购买服务 ②三种部署模式定义 ③IaaS/PaaS/SaaS 区别 +「可操控性最高＝IaaS」④专有云＝私有云
> 💼 销售话术：客户嫌上云贵 → 用"自建机房要养机房+硬件+软件+一堆人、还扩容慢"对比，云像用电一样按需买。

### 五、课时2–3 PPT 精华：阿里云产品全家福 📷

**阿里云发展历程（关键年份，可能考）**
2009 成立 → 2013 世界首个对外提供 5K 云计算 → 2014 中国首家提供全球云服务 → 2017 奥运会指定云 → 2019 升级"阿里云智能"、核心系统 100% 上云、平头哥含光 800 AI 芯片

**六大产品类别（先记"哪个产品属于哪一类"＝治你错题）**
| 类别 | 旗舰产品 |
|---|---|
| 弹性计算 | ECS、弹性裸金属(神龙)、GPU/FPGA、弹性伸缩、资源编排 |
| 存储 | 对象存储 OSS、块存储、文件存储 NAS、归档存储 |
| 网络 | 专有网络 VPC、负载均衡 SLB、NAT 网关、弹性公网 IP、云企业网、VPN 网关 |
| 数据库 | PolarDB、RDS(MySQL等)、Redis、MongoDB、DTS/DBS/DMS |
| CDN与边缘 | CDN、SCDN 安全加速、DCDN 全站加速 |
| 安全 | DDoS 高防、WAF、云安全中心、云防火墙 |

**🔥 存储三兄弟（高频考点 + 你 Q5 错因）**
- **OSS 对象存储**：海量非结构化大文件(图片/视频/备份)，比传统存储省 25%~75% → "存了不常改"
- **块存储**：块级**随机**读写、高性能高可靠 → 给 ECS 当系统盘/数据盘 ← ⚠️"随机读写"是它，不是 OSS！
- **NAS 文件存储**：支持 NFS/CIFS 协议，多台机器**共享**文件

**弹性计算补充**
- 神龙(弹性裸金属)：**虚拟机的弹性 + 物理机的性能**、物理隔离、分钟交付 ⭐
- 弹性伸缩：自动增减计算资源（双 11 自动扩容就靠它）

**网络补充**
- VPC＝逻辑隔离网络 ｜ SLB＝多台 ECS 流量分发 ｜ NAT 网关＝转发/共享带宽 ｜ EIP＝独立公网 IP ｜ 云企业网＝跨地域企业级互联 ｜ VPN 网关＝云上云下加密互通

> 🎯 考点：①产品归类（哪个属于哪类）②存储三兄弟区别 ③神龙特点
> 💼 销售：PolarDB 是阿里**自研**关系型数据库（对标 Oracle / AWS Aurora）；神龙卖给"既要性能又要弹性"的客户
> 📌 注意：**RAM（你 Q9 错题）不在产品全家福里**——它是"访问控制/账号权限"，属安全管理，阶段三讲

### 六、课时4–5 PPT 精华：数据库 + 体系架构 + 地域可用区 📷

**数据库类产品（你 SQL 背景的主场）**
- **RDS**：托管关系型数据库；RDS MySQL 版最常用（LAMP 经典组合里的 M）
- **PolarDB**：阿里**自研**云数据库，兼容 MySQL/PostgreSQL、兼容 Oracle 语法，性能最高达传统 MySQL **6 倍**，存储可达 100T —— 💼 卖点：高性能+兼容性，替换 Oracle 降本
- **DTS** 数据传输服务：数据库间**迁移/同步/订阅**（上云搬数据靠它）
- **DMS** 数据管理：网页版数据库管理工具（比 Navicat 易用）

**体系架构（了解即可，注意老名词）**
- 底层：数据中心 → Linux → 分布式文件系统/任务调度/资源管理 → 上层 ECS/OSS/RDS/ODPS/OTS
- ⚠️ 老 PPT 名词：**ODPS ＝ 现在的 MaxCompute**（大数据）、**OTS ＝ 现在的 TableStore**（结构化）——你做数据分析/AI 会用到 MaxCompute

**🔥 地域 Region & 可用区 Zone（高频考点，必背）**
- 地域 ＝ 按地理位置划分；**资源创建后不能修改地域**（⚠️ 高频陷阱题）；不同地域资源略有差异
- 可用区 ＝ 同地域内**电力/网络独立**的物理区域；同可用区内实例延时更小
- 关系：**地域完全独立；可用区完全隔离，但同地域内可用区之间低时延互联**
- 选地域考虑：①目标用户地理位置 ②产品关系 ③价格 ④**备案**（国内业务，中国特色考点）
- 选可用区考虑：**容灾能力 ↔ 网络延时** 权衡（要容灾就跨可用区，要低延时就同可用区）

> 🎯 必考：①地域创建后不可改 ②可用区＝故障隔离+低时延互联（→多可用区做高可用）③选地域要备案

### 七、完整服务体系 + 第1章总结 📷

**阿里云完整服务体系（了解，偏销售）**
以用户(国内/国际)为中心的 6 大支撑：自助服务(帮助文档/社区/工具) · 技术支持(工单/架构/解决方案/护航) · 服务运营 · 培训认证 · 生态服务(云市场) · 电话支持
💼 销售用：阿里云卖的不只是产品，还有"全天候工单 + 帮助中心 + 开发者社区 + 云市场生态 + 培训认证"一整套服务——客户买的是**安心**。

**✅ 第 1 章完成**，覆盖：云概念 / 部署模式 / 服务模式 / 阿里云主要产品 / 地域可用区 / 选地域可用区
**课程思考题**（用作自测）：①云 vs 传统 IT 的优势？ ②如何选地域和可用区？

### 📌 今日错题（详见 `错题本.md`）
- ECS ≠ 对象存储 → ECS 是**弹性计算/服务器**
- OSS ≠ 数据库随机读写 → OSS 存**海量非结构化大文件**
- RDS ≠ 负载均衡 → RDS 是**托管关系型数据库**
- RAM = **访问控制 / 权限管理**

### 八、Day1 测验讲解 · 重点弱项 🎯（必须吃透）

**测验：客观题 6/10**（地域可用区 / OSS / SLB / 神龙 全对；以下 4 点要补）

**存储三兄弟（彻底分清）**
| 类型 | 像什么 | 给谁用 | 关键词 |
|---|---|---|---|
| 对象存储 OSS | 无限网盘 | 海量文件，存了不常改 | 图片/视频/备份、API 访问 |
| **块存储(云盘)** | 电脑硬盘 | 挂给**一台** ECS 当系统盘/数据盘 | **随机读写**、挂载、块级 |
| **文件存储 NAS** | 共享文件夹 | **多台** ECS 共享同一份文件 | **NFS/CIFS**、共享 |

**⚠️ RDS（反复错2次，死磕它）**
RDS = Relational **Database** Service = 关系型数据库服务，**D 就是 Database**。你写的 SQL 跑在 MySQL 里，RDS 就是阿里云**托管的 MySQL/SQLServer/PG**。它属**数据库类**，跟"流量分发"无关——那是 SLB。**看到 RDS 就想 MySQL。**

**⚠️ RAM（反复2次未答，死磕它）**
RAM = Resource **Access** Management = 访问控制。作用：建**子账号**、分**权限**，控制"谁能访问哪些资源"（像公司**门禁卡**）。考"子账号/最小权限"就是它。

**场景题三个必记概念**
- **ICP 备案**：国内网站/对外服务必须备案（选国内地域要考虑）
- **弹性伸缩 + 按量付费**：波动流量（如大促）自动扩缩、只为高峰付费 =「扛得住又不浪费」
- **多可用区容灾**：单机房故障 → 跨**可用区**部署 + SLB 分流，业务不挂（是"多可用区"，不是"多地存储"）

### 九、概念答疑（学员追问 · 挖根）🔍
- **Relational 关系型**：数据存在"表"(行+列)里，表与表通过共同字段(如用户ID)建立"关系"，SQL 的 `JOIN` 就是用这个关系。MySQL/SQLServer/PG 都是关系型；RDS 托管它们。对比 NoSQL(Redis/MongoDB)非表格。**会 SQL ＝ 已懂关系型**。
- **ECS** = Elastic Compute Service 弹性计算服务（云服务器）。
- **OSS** = Object Storage Service 对象存储：把文件当"对象"，通过 URL/API 存取。
- **存储选型**：成品文件给人看/海量不常改 → OSS；当硬盘给程序随机读写 → 块存储；多台共享文件 → NAS；长期归档最便宜 → 归档存储。
- **NAS / NFS / CIFS**：NAS＝网络共享文件夹(设备)；NFS＝Linux 共享协议、CIFS(SMB)＝Windows 共享协议（开柜子的两把钥匙，NAS 两种都支持）。

- **Redis**（REmote DIctionary Server）：**内存键值**数据库，极快；做缓存/排行榜/秒杀计数/会话。阿里云：云数据库 Redis（Tair）。
- **MongoDB**（名源自 humongous 巨大）：**文档**数据库(类 JSON)，结构灵活；做多变属性/日志/用户画像。阿里云：云数据库 MongoDB 版。
- ☝️ Redis、MongoDB 都是 **NoSQL(非关系型)**，对比 MySQL/RDS 的"表格+关系"。
- **为什么要对象存储**：互联网时代图片/视频/备份等**海量非结构化文件**爆炸 → OSS 弹性近无限、便宜、高可靠(多副本)、自带 URL 好分发(配 CDN)、免运维；扁平 key 寻址，海量也不卡。
- **NFS** = Network File System（Sun 1984，Linux/Unix 标准）；**CIFS** = Common Internet File System（SMB 协议的版本，微软，Windows 标准）。

- **为什么有文件共享协议**：让多台机器通过网络共享**同一份**文件（不用各存一份、避免不同步）。协议＝机器间"对话规矩"，负责 找文件/读写/加锁防冲突/权限。NFS(Linux)、CIFS(Windows) 是两套规矩，NAS 两种都支持。HTTP 也是协议（OSS 走它，按网址取整个文件，不挂载成文件夹）——这正是 OSS 与 NAS 的本质区别。

### 十、缩写记忆法 · 锚点字母 🧩（专治"反应慢"）
核心：缩写不是无意义字母——**抓一个"锚点字母"就知道它干嘛**。

| 缩写 | 锚点 | 含义 | 所以是 |
|---|---|---|---|
| E**C**S | C | Compute 计算 | 服务器 |
| O**S**S | S | Storage 存储 | 对象存储 |
| R**D**S | D | Database 数据库 | 数据库（不是网络！） |
| S**L**B | L | Load 负载 | 负载均衡 |
| V**P**C | P | Private 私有 | 私有网络 |
| R**A**M | A | Access 访问 | 权限控制 |
| C**D**N | D | Delivery 分发 | 内容分发 |
| N**A**S | S(Storage) | 网络共享存储 | 文件存储 |

**3 步习惯**：看到缩写 → ①念全称英文 ②抓锚点字母 ③说中文功能。
**串成一句话**：用户 → SLB(分流) → 多台 ECS(服务器) → 查 RDS(数据库)/取 OSS(图片)，都在 VPC(网络)里，RAM(权限)管谁能动。
**方法**：别重读，要 **自测 + 间隔重复**（每次开课前 30 秒"缩写快闪"）。

### 十一、SLB 负载均衡（学员追问）⚖️
- 锚点：S**L**B → **L**=Load 负载 → 负载均衡。属**网络类**。
- 是什么：站在用户和多台 ECS 中间的"流量分发器"，用户只看到一个入口(SLB 地址)。
- 解决两件事：①**分摊压力**(多台 ECS 合力扛高流量) ②**高可用**(某台挂了，靠**健康检查**自动剔除、流量导给好机器，用户无感)。
- 经典架构：**用户 → SLB → 多台 ECS**，配**弹性伸缩**按需增减机器。
- 类比：餐厅领位员，把客人均匀安排到不同空桌。
- 💼 面试："就一台服务器为何要 SLB？"→ 扛流量(可扩展) + 不宕机(高可用) + 配弹性伸缩省成本。

### ✅ 今日状态
**Day 1 完成**，测验 6/10；弱项（块存储/NAS/RDS/RAM/场景概念）已讲解记录。下一步 **Day 2：ECS 深入**。
